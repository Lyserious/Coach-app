<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Coach_app.ViewModels.Groups"
             x:Class="Coach_app.Views.Groups.SessionDetailView"
             x:Name="SessionPage"
             Title="{Binding GroupName}"
             BackgroundColor="{StaticResource BrandDarkBlue}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *">

        <VerticalStackLayout Grid.Row="0" BackgroundColor="{StaticResource BrandNavyBlue}">
            <Grid ColumnDefinitions="*, Auto" Padding="20,20,20,10">
                <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                    <Label Text="{Binding DateDisplay}" FontSize="18" FontFamily="OpenSansSemibold" TextColor="{StaticResource NeutralWhite}"/>
                    <Label Text="{Binding GroupName}" FontSize="12" TextColor="{StaticResource NeutralLightGray}"/>
                </VerticalStackLayout>
                <Button Grid.Column="1" Text="💾" Command="{Binding SavePerformancesCommand}" IsVisible="{Binding IsPerformanceVisible}" BackgroundColor="Transparent" TextColor="{StaticResource BrandCoral}" FontSize="20" Padding="0"/>
            </Grid>

            <Grid ColumnDefinitions="*,*,*" Margin="0,10,0,0">
                <Button Grid.Column="0" Text="Programme" Command="{Binding SetTabCommand}" CommandParameter="0" BackgroundColor="Transparent" TextColor="{Binding TabProgramColor}" FontFamily="OpenSansSemibold" FontSize="12"/>
                <Button Grid.Column="1" Text="Performances" Command="{Binding SetTabCommand}" CommandParameter="1" BackgroundColor="Transparent" TextColor="{Binding TabPerfColor}" FontFamily="OpenSansSemibold" FontSize="12"/>
                <Button Grid.Column="2" Text="Appel" Command="{Binding SetTabCommand}" CommandParameter="2" BackgroundColor="Transparent" TextColor="{Binding TabAttendanceColor}" FontFamily="OpenSansSemibold" FontSize="12"/>
                <BoxView Grid.Column="{Binding CurrentTab}" VerticalOptions="End" HeightRequest="3" Color="{StaticResource BrandCoral}"/>
            </Grid>
        </VerticalStackLayout>

        <Grid Grid.Row="1" IsVisible="{Binding IsProgramVisible}">
            <Grid RowDefinitions="Auto, *, Auto">
                <Border Grid.Row="0" Margin="15,15,15,0" Padding="15" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 12" StrokeThickness="0">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="OBJECTIFS / DESCRIPTION" FontSize="11" TextColor="{StaticResource BrandOrange}" FontFamily="OpenSansSemibold"/>
                        <Editor Text="{Binding SessionDescription}" Placeholder="Description de la séance..." AutoSize="TextChanges" MaximumHeightRequest="100" FontSize="14" TextColor="{StaticResource NeutralWhite}" PlaceholderColor="{StaticResource NeutralGray}" BackgroundColor="Transparent"/>
                        <Button Text="Enregistrer le texte" Command="{Binding SaveDescriptionCommand}" FontSize="11" HeightRequest="30" Padding="15,0" BackgroundColor="{StaticResource BrandDarkBlue}" TextColor="{StaticResource NeutralLightGray}" HorizontalOptions="End" CornerRadius="8"/>
                    </VerticalStackLayout>
                </Border>

                <CollectionView Grid.Row="1" ItemsSource="{Binding SessionExercises}" Margin="15,10">
                    <CollectionView.EmptyView>
                        <Label Text="Aucun exercice ajouté." HorizontalOptions="Center" VerticalOptions="Center" TextColor="{StaticResource NeutralLightGray}"/>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,5" Padding="15" StrokeShape="RoundRectangle 12" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeThickness="0">
                                <Grid ColumnDefinitions="*, Auto">
                                    <VerticalStackLayout Spacing="5">
                                        <Label Text="{Binding Exercise.Name}" FontFamily="OpenSansSemibold" TextColor="{StaticResource NeutralWhite}" FontSize="15"/>
                                        <Label Text="{Binding Exercise.ScoringType}" FontSize="11" TextColor="{StaticResource NeutralLightGray}"/>
                                        <HorizontalStackLayout Spacing="8" Margin="0,5,0,0">
                                            <Entry Text="{Binding Sets}" Placeholder="Sets" WidthRequest="45" FontSize="14" HorizontalTextAlignment="Center" TextColor="{StaticResource NeutralWhite}" BackgroundColor="{StaticResource BrandDarkBlue}"/>
                                            <Label Text="x" VerticalOptions="Center" TextColor="{StaticResource NeutralGray}"/>
                                            <Entry Text="{Binding Reps}" Placeholder="Reps" WidthRequest="45" FontSize="14" HorizontalTextAlignment="Center" TextColor="{StaticResource NeutralWhite}" BackgroundColor="{StaticResource BrandDarkBlue}"/>
                                            <Entry Text="{Binding Weight}" Placeholder="Kg" WidthRequest="45" FontSize="14" HorizontalTextAlignment="Center" TextColor="{StaticResource NeutralWhite}" BackgroundColor="{StaticResource BrandDarkBlue}"/>
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>
                                    <VerticalStackLayout Grid.Column="1" Spacing="10" VerticalOptions="Center">
                                        <Button Text="✕" Command="{Binding Source={x:Reference SessionPage}, Path=BindingContext.DeleteExerciseCommand}" CommandParameter="{Binding .}" BackgroundColor="Transparent" TextColor="{StaticResource BrandCoral}" HeightRequest="35" WidthRequest="35" Padding="0"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Grid Grid.Row="2" ColumnDefinitions="*, Auto" Padding="15" ColumnSpacing="15">
                    <Button Grid.Column="0" Text="+ Ajouter Exercice" Command="{Binding AddExerciseCommand}" BackgroundColor="{StaticResource BrandCoral}" TextColor="{StaticResource NeutralWhite}" CornerRadius="12" FontFamily="OpenSansSemibold"/>
                    <Button Grid.Column="1" Text="📥" Command="{Binding ImportTemplateCommand}" BackgroundColor="{StaticResource BrandNavyBlue}" TextColor="{StaticResource NeutralWhite}" WidthRequest="50" CornerRadius="12"/>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="1" IsVisible="{Binding IsPerformanceVisible}" RowDefinitions="Auto, *">
            <CollectionView Grid.Row="0" ItemsSource="{Binding SessionExercises}" HeightRequest="65" SelectionMode="Single" SelectedItem="{Binding SelectedExerciseForPerf, Mode=TwoWay}" HorizontalScrollBarVisibility="Never" Margin="0,5">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 20" StrokeThickness="0" BackgroundColor="{StaticResource BrandNavyBlue}" Padding="20,0" Margin="10,5">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="{StaticResource BrandCoral}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Label Text="{Binding Exercise.Name}" VerticalOptions="Center" FontFamily="OpenSansSemibold" TextColor="{StaticResource NeutralWhite}"/>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <CollectionView Grid.Row="1" ItemsSource="{Binding StudentPerformances}" Margin="15,0">
                <CollectionView.EmptyView>
                    <Label Text="Sélectionnez un exercice ci-dessus" HorizontalOptions="Center" VerticalOptions="Center" TextColor="{StaticResource NeutralLightGray}"/>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,5" Padding="15" StrokeShape="RoundRectangle 12" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeThickness="0">
                            <Grid ColumnDefinitions="Auto, *, Auto">
                                <Border Grid.Column="0" HeightRequest="45" WidthRequest="45" StrokeShape="RoundRectangle 22" StrokeThickness="0" Margin="0,0,15,0" VerticalOptions="Center">
                                    <Image Source="{Binding PhotoPath, Converter={StaticResource StringNullToImageConverter}}" Aspect="AspectFill"/>
                                </Border>
                                <Label Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center" FontFamily="OpenSansSemibold" TextColor="{StaticResource NeutralWhite}"/>

                                <Grid Grid.Column="2" WidthRequest="130">
                                    <Grid IsVisible="{Binding IsNumeric}" ColumnDefinitions="Auto, *, Auto">
                                        <Button Grid.Column="0" Text="-" Command="{Binding Source={x:Reference SessionPage}, Path=BindingContext.DecrementScoreCommand}" CommandParameter="{Binding .}" HeightRequest="35" WidthRequest="35" Padding="0" BackgroundColor="{StaticResource BrandDarkBlue}" TextColor="{StaticResource NeutralWhite}" CornerRadius="8"/>
                                        <Entry Grid.Column="1" Text="{Binding ValueDisplay}" HorizontalTextAlignment="Center" Keyboard="Numeric" BackgroundColor="Transparent" TextColor="{StaticResource NeutralWhite}"/>
                                        <Button Grid.Column="2" Text="+" Command="{Binding Source={x:Reference SessionPage}, Path=BindingContext.IncrementScoreCommand}" CommandParameter="{Binding .}" HeightRequest="35" WidthRequest="35" Padding="0" BackgroundColor="{StaticResource BrandDarkBlue}" TextColor="{StaticResource NeutralWhite}" CornerRadius="8"/>
                                    </Grid>
                                    <Switch IsToggled="{Binding IsCompleted}" IsVisible="{Binding IsCompletion}" HorizontalOptions="End" OnColor="{StaticResource BrandCoral}"/>
                                    <Entry Text="{Binding ValueDisplay}" Placeholder="Niveau" IsVisible="{Binding IsLevel}" HorizontalOptions="Fill" TextColor="{StaticResource NeutralWhite}"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>

        <Grid Grid.Row="1" IsVisible="{Binding IsAttendanceVisible}">
            <Grid RowDefinitions="*, Auto">
                <CollectionView Grid.Row="0" ItemsSource="{Binding AttendanceList}" Margin="15,10">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15" Margin="0,5" StrokeShape="RoundRectangle 12" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeThickness="0">
                                <Grid ColumnDefinitions="Auto, *, Auto">
                                    <Border Grid.Column="0" HeightRequest="45" WidthRequest="45" StrokeShape="RoundRectangle 22" StrokeThickness="0" Margin="0,0,15,0">
                                        <Image Source="{Binding PhotoPath, Converter={StaticResource StringNullToImageConverter}}" Aspect="AspectFill"/>
                                    </Border>
                                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                        <Label Text="{Binding DisplayName}" FontFamily="OpenSansSemibold" TextColor="{StaticResource NeutralWhite}" FontSize="15"/>
                                    </VerticalStackLayout>

                                    <Button Grid.Column="2" Text="{Binding Status}" Command="{Binding Source={x:Reference SessionPage}, Path=BindingContext.CycleStatusCommand}" CommandParameter="{Binding .}" HeightRequest="35" FontSize="11" CornerRadius="8" BackgroundColor="{Binding Status, Converter={StaticResource StatusToColorConverter}}" TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" Padding="15,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <Button Grid.Row="1" Text="Enregistrer Appel" Command="{Binding SaveAttendanceCommand}" BackgroundColor="{StaticResource BrandCoral}" TextColor="{StaticResource NeutralWhite}" Margin="20" CornerRadius="25" HeightRequest="50" FontFamily="OpenSansSemibold"/>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>