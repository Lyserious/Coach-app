<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Coach_app.ViewModels.Groups"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Coach_app.Views.Groups.SessionDetailView"
             Title="Séance">

    <Grid RowDefinitions="Auto, *">

        <Frame Grid.Row="0" BackgroundColor="{StaticResource Primary}" CornerRadius="0" Padding="15" HasShadow="False">
            <VerticalStackLayout Spacing="10">
                <Label Text="{Binding GroupName}" TextColor="White" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                <Label Text="{Binding DateDisplay}" TextColor="White" FontSize="14" Opacity="0.9" HorizontalOptions="Center"/>

                <Button Text="📋 Faire l'Appel" Command="{Binding ToggleAttendanceVisibilityCommand}"
                        BackgroundColor="White" TextColor="{StaticResource Primary}"
                        CornerRadius="20" HeightRequest="40" FontAttributes="Bold" Margin="0,5,0,0"/>
            </VerticalStackLayout>
        </Frame>

        <ScrollView Grid.Row="1" Padding="15">
            <VerticalStackLayout Spacing="20">

                <VerticalStackLayout IsVisible="{Binding IsAttendanceVisible}" Spacing="10">
                    <Label Text="Liste des élèves" FontAttributes="Bold" FontSize="16"/>

                    <CollectionView ItemsSource="{Binding AttendanceList}" HeightRequest="300">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Margin="0,0,0,10" Padding="10" CornerRadius="10" HasShadow="True" BorderColor="Transparent">
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SessionDetailViewModel}}, Path=CycleStatusCommand}" CommandParameter="{Binding .}"/>
                                    </Frame.GestureRecognizers>

                                    <Frame.Triggers>
                                        <DataTrigger TargetType="Frame" Binding="{Binding Status}" Value="Present">
                                            <Setter Property="BackgroundColor" Value="#E8F5E9"/>
                                            <Setter Property="BorderColor" Value="Green"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Frame" Binding="{Binding Status}" Value="Absent">
                                            <Setter Property="BackgroundColor" Value="#FFEBEE"/>
                                            <Setter Property="BorderColor" Value="Red"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Frame" Binding="{Binding Status}" Value="Late">
                                            <Setter Property="BackgroundColor" Value="#FFF3E0"/>
                                            <Setter Property="BorderColor" Value="Orange"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Frame" Binding="{Binding Status}" Value="{x:Null}">
                                            <Setter Property="BackgroundColor" Value="White"/>
                                            <Setter Property="BorderColor" Value="Transparent"/>
                                        </DataTrigger>
                                    </Frame.Triggers>

                                    <Grid ColumnDefinitions="50, *, Auto">
                                        <Frame Grid.Column="0" HeightRequest="40" WidthRequest="40" CornerRadius="20" Padding="0" IsClippedToBounds="True">
                                            <Image Source="{Binding PhotoPath, Converter={StaticResource StringNullToImageConverter}}" Aspect="AspectFill"/>
                                        </Frame>

                                        <Label Grid.Column="1" Text="{Binding DisplayName}" VerticalOptions="Center" Margin="10,0" FontAttributes="Bold" TextColor="Black"/>

                                        <Label Grid.Column="2" FontSize="24" VerticalOptions="Center">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Present">
                                                    <Setter Property="Text" Value="✅"/>
                                                </DataTrigger>
                                                <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Absent">
                                                    <Setter Property="Text" Value="❌"/>
                                                </DataTrigger>
                                                <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="Late">
                                                    <Setter Property="Text" Value="⏰"/>
                                                </DataTrigger>
                                                <DataTrigger TargetType="Label" Binding="{Binding Status}" Value="{x:Null}">
                                                    <Setter Property="Text" Value="⚪"/>
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <Button Text="Enregistrer" Command="{Binding SaveCommand}" BackgroundColor="{StaticResource Primary}" TextColor="White"/>
                    <BoxView HeightRequest="1" Color="LightGray" Margin="0,10"/>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="Contenu de la séance" FontAttributes="Bold" FontSize="18"/>
                    <Frame BackgroundColor="#F5F5F5" Padding="20" CornerRadius="10">
                        <Label Text="Ici, tu pourras bientôt ajouter tes exercices, importer une séance type ou écrire tes notes." 
                               TextColor="Gray" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>
                    </Frame>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>