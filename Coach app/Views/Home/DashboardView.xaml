<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Coach_app.ViewModels.Home"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Coach_app.Views.Home.DashboardView"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource BrandDarkBlue}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing" Command="{Binding LoadSessionsCommand}" />
    </ContentPage.Behaviors>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="30">

            <Grid ColumnDefinitions="Auto, *, Auto" VerticalOptions="Center" ColumnSpacing="15">

                <Border Grid.Column="0"
                        Stroke="{StaticResource BrandCoral}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 10"
                        HeightRequest="40"
                        WidthRequest="40"
                        BackgroundColor="{StaticResource BrandNavyBlue}"
                        IsVisible="{OnPlatform Android=True, iOS=True, WinUI=False, MacCatalyst=False}">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OpenMenuCommand}"/>
                    </Border.GestureRecognizers>
                    <Label Text="☰" 
                           TextColor="{StaticResource BrandCoral}" 
                           FontSize="20" 
                           HorizontalOptions="Center" 
                           VerticalOptions="Center"/>
                </Border>

                <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                    <Label Text="{Binding WelcomeMessage}" 
                           FontSize="24" 
                           FontFamily="OpenSansSemibold" 
                           TextColor="{StaticResource BrandCoral}"
                           LineBreakMode="TailTruncation"/>
                    <Label Text="C'est parti pour une nouvelle journée!" 
                           TextColor="{StaticResource NeutralLightGray}" 
                           FontSize="12"/>
                </VerticalStackLayout>

                <Border Grid.Column="2"
                        Stroke="{StaticResource BrandCoral}"
                        StrokeThickness="2"
                        StrokeShape="RoundRectangle 30" 
                        HeightRequest="50"
                        WidthRequest="50"
                        HorizontalOptions="End"
                        VerticalOptions="Center"
                        BackgroundColor="{StaticResource BrandNavyBlue}">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToPageCommand}" CommandParameter="SettingsView"/>
                    </Border.GestureRecognizers>
                    <Image Source="dotnet_bot.png" Aspect="AspectFill"/>
                </Border>
            </Grid>

            <Border Stroke="Transparent"
                    BackgroundColor="{StaticResource BrandNavyBlue}"
                    StrokeShape="RoundRectangle 25" 
                    Padding="15">

                <VerticalStackLayout Spacing="15">
                    <Grid ColumnDefinitions="Auto, *, Auto">
                        <Button Grid.Column="0" Text="◀" Command="{Binding PreviousDayCommand}" 
                                BackgroundColor="Transparent" TextColor="{StaticResource BrandCoral}" 
                                FontSize="22" HeightRequest="40" WidthRequest="40" Padding="0" BorderColor="Transparent"/>

                        <Label Grid.Column="1" Text="{Binding DateDisplay}" 
                               TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" 
                               FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>

                        <Button Grid.Column="2" Text="▶" Command="{Binding NextDayCommand}" 
                                BackgroundColor="Transparent" TextColor="{StaticResource BrandCoral}" 
                                FontSize="22" HeightRequest="40" WidthRequest="40" Padding="0" BorderColor="Transparent"/>
                    </Grid>

                    <BoxView HeightRequest="1" Color="{StaticResource BrandDarkBlue}" Opacity="0.3"/>

                    <CollectionView ItemsSource="{Binding TodaysSessions}" HeightRequest="120">
                        <CollectionView.EmptyView>
                            <VerticalStackLayout Spacing="5" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,15">
                                <Label Text="🏖️" FontSize="28" HorizontalOptions="Center" Opacity="0.7"/>
                                <Label Text="Aucune séance prévue" TextColor="{StaticResource NeutralLightGray}" FontSize="14"/>
                            </VerticalStackLayout>
                        </CollectionView.EmptyView>

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,10" Padding="12" StrokeShape="RoundRectangle 15" 
                                        BackgroundColor="{StaticResource BrandDarkBlue}"
                                        StrokeThickness="0">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:DashboardViewModel}}, Path=GoToSessionCommand}" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                    <Grid ColumnDefinitions="65, *, Auto">
                                        <Label Grid.Column="0" Text="{Binding TimeDisplay}" FontSize="13" FontFamily="OpenSansSemibold" VerticalOptions="Center" TextColor="{StaticResource BrandOrange}"/>
                                        <Label Grid.Column="1" Text="{Binding GroupName}" FontSize="15" VerticalOptions="Center" TextColor="{StaticResource NeutralWhite}" LineBreakMode="TailTruncation"/>
                                        <Label Grid.Column="2" Text="›" TextColor="{StaticResource NeutralGray}" FontSize="20" VerticalOptions="Center" Margin="0,-3,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Spacing="15">
                <Label Text="ACCÈS RAPIDE" FontFamily="OpenSansSemibold" FontSize="14" TextColor="{StaticResource NeutralLightGray}" CharacterSpacing="1" Margin="5,0,0,0"/>

                <Grid ColumnDefinitions="*,*" RowDefinitions="120, 120, 120" ColumnSpacing="15" RowSpacing="15">

                    <Border Grid.Row="0" Grid.Column="0" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToPageCommand}" CommandParameter="//StudentLibraryView"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="🎓" FontSize="36" HorizontalOptions="Center"/>
                            <Label Text="Élèves" TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="0" Grid.Column="1" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToPageCommand}" CommandParameter="//Groups"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="👥" FontSize="36" HorizontalOptions="Center"/>
                            <Label Text="Groupes" TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToPageCommand}" CommandParameter="//Exercises"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="📚" FontSize="36" HorizontalOptions="Center"/>
                            <Label Text="Exercices" TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToPageCommand}" CommandParameter="//SessionTemplates"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="📋" FontSize="36" HorizontalOptions="Center"/>
                            <Label Text="Modèles" TextColor="{StaticResource NeutralWhite}" FontFamily="OpenSansSemibold" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="0" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40" Opacity="0.5">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="📷" FontSize="36" HorizontalOptions="Center" TextColor="{StaticResource NeutralLightGray}"/>
                            <Label Text="Photos" TextColor="{StaticResource NeutralLightGray}" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="1" BackgroundColor="{StaticResource BrandNavyBlue}" StrokeShape="RoundRectangle 40" Opacity="0.5">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                            <Label Text="🛠️" FontSize="36" HorizontalOptions="Center" TextColor="{StaticResource NeutralLightGray}"/>
                            <Label Text="Autres outils" TextColor="{StaticResource NeutralLightGray}" FontSize="15"/>
                        </VerticalStackLayout>
                    </Border>

                </Grid>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>